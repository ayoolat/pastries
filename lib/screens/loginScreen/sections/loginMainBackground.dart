import 'package:flutter/cupertino.dart';

class _loginBackground extends StatelessWidget {
  const _loginBackground({Key? key}) : super(key: key);

  void _onLoginPress() {}

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
